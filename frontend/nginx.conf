server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: proxy to backend through Nginx if desired
  # location /api/ {
  #   proxy_pass http://ecom_backend:8080;
  # }
}


